#include "stdlib.h"
#include <stdio.h>
char *result;
void recursive(int l, int r, int size) {}

int main() {
  char string[50];
  while (scanf("%s", string)) {
    printf("%s", string);
    result = NULL;
  }

  return EXIT_SUCCESS;
}
